#include <iostream>
#include <vector>

using namespace std;
enum Nucleotides {
    U,
    A,
    C,
    G
};

// constraints:
// no sharp turns: (b_i, b_j), i < j -4
// no crossing over: (b_i, b_j), (b_k, b_l), i < j < k < l

// return max number of pairings
int fold(vector<int> sequence) {
    


    return -1;
}